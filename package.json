{
  "name": "xlent",
  "version": "0.1.0",
  "private": true,
  "description": "Transform spreadsheet-based computational models into structured, inspectable, deterministic, reusable software models.",
  "workspaces": [
    "packages/core",
    "packages/api",
    "packages/web",
    "packages/sdk",
    "packages/cli"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspace=packages/api & npm run dev --workspace=packages/web",
    "test": "npm run test --workspaces",
    "lint": "eslint packages/*/src"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
